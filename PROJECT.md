# PROJECT.md

> 这份文件只记录**代码里读不出来的东西**：决策、约束、以及已经否决的方案。
> 文件清单、图片映射表、尺寸表一律不写——那些从磁盘和 git 就能读到，写进来只会过期。
>
> 最后更新：2026-09-11

## 1. 这是什么 / 怎么跑起来

个人设计作品集网站，三个项目都已完成：

| | 项目 | 类型 | 性质 |
|---|---|---|---|
| 01 | 智能冰箱 | AI 融合型产品设计 | 课程小组作业（Group F）|
| 02 | 合拍旅行 | OTA 多人旅行 AI 协作决策 | 个人项目 |
| 03 | 小凉熊 CoolItch Kids | 儿童护理产品设计 | 独立完成 |

```sh
npm install
npm run build
python3 -m http.server 4173 --bind 127.0.0.1 --directory out
```

入口 `http://127.0.0.1:4173/#project=1`。`#project=1` 只是选中第一个项目，不会自动展开详情；点封面或「探索项目」进入。

**如果 `npm run build` 报 `EDEADLK`**：项目在 iCloud 同步的桌面上，未下载的文件读不了，`cpSync public -> out/images` 会失败。只改了文案或样式（没有新增图片）时，跳过完整 build，直接重新生成首页：

```sh
node -e "import('./scripts/project-stage.mjs').then(async m=>{const {readFileSync}=await import('node:fs');const {profile,projects}=JSON.parse(readFileSync('app/content.json','utf8'));m.buildStage(profile,projects)})"
```

这条不复制图片。新增图片时需要手动 `cp public/images/<新图>.png out/images/`。

## 2. 架构：三件反直觉的事

1. **主预览不是 Next/React 生成的。** `scripts/build.mjs` 先生成 5 个静态页，最后调用 `buildStage()` 把 `out/index.html` 覆盖成真正的舞台页。工程里的 `app/page.tsx`、`components/ui/*` 等是遗留文件，不参与主预览——改它们不会有任何效果。
2. **`out/` 是产物。** 所有改动落在源码，不要直接编辑 `out/`。
3. **`/work/<slug>/` 是旧式独立页，和首页内展开的长页不同步。** 这是已知状态，不是 bug，不要"顺手修复"。当前的浏览路径是首页内展开。

## 3. 文件职责

| 文件 | 职责 |
|---|---|
| `app/content.json` | 全部文案、章节结构、图片引用 |
| `scripts/project-stage.mjs` | 生成首页与同页展开的项目长页；首页背景色数组也在这里 |
| `app/stage.css` | 首页、封面、详情、标注、响应式 |
| `scripts/stage.js` | 项目切换、展开/返回、视差与封面形变、全览轨道；展开背景色数组在这里 |
| `scripts/build.mjs` | 复制 public、生成 5 个静态页，再调 buildStage 覆盖首页 |
| `scripts/about-page.mjs` | 生成 /about/（教育背景 + 技能），数据来自 content.json 的 profile.education / profile.skills |
| `app/about.css` | 只服务 /about/。深底浅字，与首页 stage 的深色基调衔接；正文 Inter，小节标题 Georgia 斜体；年份用 Inter lining-nums |

## 4. 章节渲染器

项目一的章节是一节一个专属版式（`problem` / `strategy` / `entry` / `action` / `rewards` / `iteration` / `reflection`），写死在渲染器里，只服务项目一。

**项目二和项目三用的是通用章节 `chapters[]`**，一个类型覆盖全部。每节可选字段：

| 字段 | 渲染成什么 |
|---|---|
| `kicker` / `title` / `text` | 左标题 + 右正文的两栏标题区 |
| `columns[]` | 两栏并列的小标题 + 条目（如「设计总结 / 还没有验证的」）|
| `points[]` | 2–4 栏的要点网格，`number` 可放序号或数据 |
| `note` | 标题区下方的一段补充 |
| `cards[]` | 图 + 编号 + 标题 + 说明的并排卡片（如三版手板）|
| `steps[]` | **流程**：竖屏长截图 + 说明，左右交替 |
| `gallery[]` | 横向自由滚动的全览轨道 |
| `summary` | 居中的大字收束句 |

**渲染顺序是固定的**：标题区 → columns → points → note → cards → steps → gallery → summary。要换顺序就得改渲染器。

加新章节优先复用这套字段，**不要为单节再写专属版式**——项目二三各 6 节，全部由这一个类型渲染。

## 5. 设计约束

### 通用

- 白底黑字，编辑式排版，主次清楚。避免模板化卡片、浅灰模块、密集研究报告式版面。
- 文案自然具体。偏好「食材状态可视化」这类明确的设计表达，不要口号式的句子。
- 图片不为了突出主视觉无限放大。
- 展开封面的中文标题：较小的实心深色黑体，不描边，不跨图片切成实心/描边两段。
- 下箭头：白圆底、深绿箭头，随滚动逐渐被正文遮住，不应一直浮在边界上。

### 数字排版（踩过坑）

**数据类数字不能用 Georgia。** Georgia 是旧式数字（oldstyle figures）——`6` `8` 往上冒，`3` `5` `7` `9` 往下掉，四个数字并排会上下跳。Georgia 字体本身没有等高数字，CSS 开关救不回来。

数据一律用：`font-family:Inter...; font-variant-numeric:lining-nums tabular-nums`。`.chapter-num` 和 `.step-num` 已经这么写了。

### 界面素材

- **项目一、项目二的界面统一 780×1688（比例 0.462）。** 引入新图前先验尺寸。
- 项目二的流程截图是长页截图，宽度 786、高度 1704–2694 不等——**这种不等高的图不要放进横向轨道**，会参差；用 `steps` 的左右交替版式。
- 从 Figma 导出前勾上「裁剪内容」，否则画框里溢出的元素会撑大导出尺寸。
- **不要把带文字的整块排版稿导成图片。** 文字在网页上写，只导纯图像的部分——否则手机上看不清、不能选中、改一个字要回 Figma。

### 封面

首页卡片会被裁成接近正方形（`35vw × 57vh`），所以封面图必须：

1. **铺满**——不能有大面积空白，否则卡片看起来是空的
2. **主体居中**——左右会被裁掉
3. **小尺寸可读**——卡片只有约 500px 宽

三个项目刻意用了三种视觉语言，各自贴合项目性质：**摄影**（食物场景）、**图形插画**（软件协作，没有实物可拍）、**产品渲染**（硬件）。

### 配色方法

**每个项目的色从自己的素材里取，再提纯**——保留色相，拉高饱和度、调整明度到该档位。直接吸管取色会发闷。

两个硬约束：

- **首页背景承载浅色文字**（`#e3e8e1`），必须够深：明度 30–38%，饱和度 25–38%
- **展开背景承载深色文字**，必须够浅：明度 85–90%。但**不能太接近纯白**，否则往下滚进白色正文时那层过渡就消失了

还有两条：

- **相邻项目要错开色相。** 现在是 靛蓝 222° → 墨绿 165° → 摩卡 26°。曾经把项目二设成靛蓝 238°，跟当时项目一的 201° 太近，滚过去像同一个色，已改。
- **素材没有颜色时，不要硬取。** 项目一原本是 `#3c667d`（201°），取自封面里冰箱内胆那片"冷色"。后来按色相统计整张图才发现：暖色占压倒性多数（40° 柠檬金 8400 px、70–80° 生菜绿 5000 px），而那片冷色区域**饱和度只有 0.03**，根本是无彩色的灰——等于从没有颜色的地方取了个颜色，所以一直显得像默认企业蓝。改成 `#314572`（222°）后靠**补色对比**成立：素材是暖的，蓝紫让柠檬和生菜跳出来。结论是，当素材本身没有可用色相时，走对比比硬凑呼应更可靠。
- **同一个色值可能散落在多处。** 换项目一的蓝时要同时改三个地方：`project-stage.mjs` 的 `colors[0]`、`stage.css` 里 `body{background}`（JS 跑之前的首屏底色，不改会闪一帧旧色）、`.cover-placeholder` 的文字色。
- **饱和度低于 20% 会发浑。** 三个项目原本的占位色都是 S 13–16%，看着像脏灰，全部提到 25% 以上才立得住。

### 换页滑动（首页 ↔ about）

一块 `.page-veil` 全屏遮罩，用 `clip-path: inset()` 从**右往左**扫过，骨白 `#EDE7DC`。离开时盖上 0.36s，跳转延时 0.38s（必须 ≥ 盖上时长，早了会在没盖满时换页露出闪烁），落地后继续往左退开 0.5s。全程 0.86s。

三件容易踩的事：

- **遮罩色不能等于目的地底色。** 最早用纯黑，about 页也是纯黑，结果退开那半程什么都看不见，只剩"首页消失了"。两个页面都深色时，往亮走明度差最大。
- **退开后要瞬间跳回右边缘**（`idle` 状态，`transition:none`），否则下次点击会从左边扩出来，方向就反了。那时它全透明收起，这一跳看不见。
- **无 JS 时必须不启用。** 靠 `<head>` 里一行内联脚本给 `<html>` 加 `anim` 类，所有遮住屏幕的样式都挂在 `html.anim` 下。没有这个类，遮罩永远是收起的，页面不会被盖死。`prefers-reduced-motion` 直接摘掉 `anim`。

两个页面的时长和颜色必须一致（`app/about.css` 与 `app/stage.css` 各有一份），不然进出会闪两种颜色。

### 封面的滚动形变

往下滚时封面会**从两侧被裁掉**并轻微顺时针旋转。参数量自参考站四个滚动位置，是**线性**的，没有缓动：

- 封面每上移 1px → 顺时针 `0.0358°`，每侧裁掉 `0.0382%` 宽度
- 常量在 `scripts/stage.js` 顶部：`COVER_ROT_PER_PX` / `COVER_CROP_PER_PX` / `COVER_CROP_MAX`

**这是 `clip-path: inset()` 裁切，不是 `scaleX`。** 两者观感完全不同：`scaleX` 是把画面挤扁（人脸变瘦），裁切是内容原地不动、边缘被吃掉。参考站是后者，像素比对证实过。

## 6. 已经否决的方案 —— 不要再提

这些都试过并且被否掉了。不知道就会重来一遍，用户得再否一次。

- 大图、左图右文、中文描边标题（历史对话里有多版，全部作废）
- 938×1846 带外阴影留边的导出，配逐图 CSS 裁剪 hack。素材已重新导出为 780×1688 无留边，**不要再写这类裁剪覆盖**
- 全览做成轮播（一次一张），或纵向滚动驱动横向位移的滚动劫持
- 全览使用 `scroll-snap` 吸附：吸附点与左侧留白对不上，会把用户从第一张弹走，手感也黏，已移除
- 全览下方的 `(01 / 11)` 计数器和左右箭头按钮：像控件，跟整站编辑气质不合，已删。键盘方向键保留
- 封面形变用 `scaleX`：观感是"挤"，不是参考站那种"消散"，已换成 clip-path
- 用界面截图当项目二的封面：卡片上只看到一块 UI，没有讲出问题
- 项目二用 Let's Ride（特斯拉拼车 App）：深蓝高密度展板，风格跟站点冲突，已换成合拍旅行
- 07 的数量调整前后对比、03 的取舍依据（Impact/Feasibility 矩阵与游戏化选型理由）：讨论过，**用户已决定不做**，不要主动提议

## 7. 证据边界 —— 硬规则

不得编造用户原话、访谈细节、测试数据或上线成效。三个项目各自可以引用的事实：

### 项目一 · 智能冰箱

- **用户访谈**：10 位同龄合租同学，半结构化访谈，便利抽样。按**提及人数**计，不是比例——写「10 位中有 7 位提到」，不写「70% 的用户」。主要提及：6 人一周买菜 ≤2 次且每次量大；7 人买到重复食材；4 人想起食材时人在上课、不在家；4 人因大份优惠多买吃不完；3 人试过记录但没动力坚持；3 人提到酸奶吃不完；4 人觉得可惜；仅 1 人买菜前会看冰箱。局限：未覆盖合租归属场景，单次访谈，样本来自同学。这场访谈未写进课程报告。
- **可用性测试**：课程报告记载 6 位参与者、任务式场景，发现首页认知过载、拖拽滑块在手机上是主要障碍；据此从 drag slider 改为 click-to-adjust 详情卡。
- **二手研究**：英国家庭贡献 70% 食物浪费、每年约 440 万吨可避免浪费、平均每户损失约 £1,000。
- **课程报告** `AUXT Coursework Group F (1).pdf` 关键页：第 5 页数字宠物避免内疚式引导；第 8 页宠物成长与长期参与；第 16 页低保真原型与 Impact/Feasibility 矩阵；第 17 页可用性测试的三个问题；第 18 页最终设计决定与预期影响。
- **游戏化是设计假设，不是研究发现。** 访谈只说明用户「没动力」维持记录，不说明积分和宠物就是答案。

### 项目二 · 合拍旅行

- 5 位受访者访谈 + 3 份真实出行群聊、111 张截图的逐条标注；旅程图部分另结合 30 份问卷。
- 群聊分析结论：54% 是无效消息；「OK！」有四种意思；三个方案从未并排出现；三份案例中都出现主导者与跟随者，其中一份发言占比 51% / 47% / 1% / 1%。
- **第 07 节「反思与下一步」是推断写的，不是用户原有材料。** 原始五页排版稿里没有反思部分。若用户未确认，不要当成她的结论引用。

### 项目三 · 小凉熊

- 30 份线上问卷（患儿家长填写）+ 一位三甲医院三十年经验皮肤科医生访谈 + 一位 8 岁患儿访谈。独立完成。
- 百分比只能是 30 的整数倍：66.7% / 83.3% / 53.3% / 40%（对应 20 / 25 / 16 / 12 人）。**不要写回小数点后一位的其他数字**，30 份样本做不出来。
- 二手研究：2019 年全球约 6514 万人受荨麻疹影响；10 岁以下儿童约 15% 会经历至少一次急性发作。

### 共同

**三个项目都没有任何实测的成效数据**——减少浪费比例、留存率、临床效果、真实用户测试，一律没有。不得写成成效。

页面上那三行边界说明（`reflection.note` 与两个 `closing.note`）**已按用户要求截短，只保留"止于什么"，删掉了"未经真实用户测试 / 没有临床测试或实际使用数据"这半句**。页面不再明写没有数据，但上面这条硬规则不变：**不得因为页面没写就把未验证的东西当成效来写**。

## 8. 素材

| 文件夹 | 内容 |
|---|---|
| `~/Desktop/ai冰箱页面` | 项目一 11 张界面的当前版本，全部 780×1688 |
| `~/Desktop/ai冰箱` | 项目一原始素材、更早的导出、课程 PDF |
| `~/Desktop/ai旅行` | 项目二的 5 页排版稿 + `ui素材/` 6 张纯净界面 |
| `~/Desktop/作品集/项目3` | 项目三的 6 页排版稿 + `照片素材/` 产品渲染与实拍 |
| `~/Desktop/作品集/项目2` | **Let's Ride，已弃用**，不要再往站点里放 |

所有文件夹都在 iCloud 同步的桌面上，未下载的文件读不了（报 `EDEADLK` 或 deadlock）。使用前先在访达里确认已下载到本地。

## 9. 已知坑

- **完整 build 在 iCloud 上会失败**，绕法见第 1 节。
- **`app/stage.css` 是多轮追加覆盖的**，新规则写在文件尾部会覆盖前面的同名规则。清理时务必保持现有视觉与响应式行为，不要大范围重构。
- **项目一 05/06 的细折线是按比例静态定位的**，换图、改裁剪或改宽高比之后需要重新校准。
- 第一次 `git add -A` 很慢（要遍历 743MB 的 node_modules，加上 iCloud 下载），几分钟属正常，不要中断。
- `.git/index.lock` 偶尔会留下残留（某些工具跑完 git 命令后删不掉锁）。`git add` 报「File exists」时 `rm -f .git/index.lock` 即可。

## 10. 当前状态与边界

- **三个项目都已定稿，用户满意。不要重构、不要"优化"文案、不要重排版式。**
- 未部署。本地改动不代表线上更新。
- Git 仓库：`bbbbbbb86/chenke-portfolio`（私有）。

**接手流程**：读这份文件 → `git log --oneline` 和 `git diff HEAD~1` 看最近改了什么 → 跑起来浏览三个项目 → 用简短中文复述你理解的架构与约束 → **等用户明确指定下一项工作**。不要自行新增章节、重新设计或部署。

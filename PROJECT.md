# PROJECT.md

> 这份文件只记录**代码里读不出来的东西**：决策、约束、以及已经否决的方案。
> 文件清单、图片映射表、尺寸表一律不写——那些从磁盘和 git 就能读到，写进来只会过期。
>
> 最后更新：2026-09-11

## 1. 这是什么 / 怎么跑起来

个人设计作品集网站。重点项目是「智能冰箱」（AI 融合型产品设计），01–08 章节已完成。项目二、三是占位。

```sh
npm install
npm run build
python3 -m http.server 4173 --bind 127.0.0.1 --directory out
```

入口 `http://127.0.0.1:4173/#project=1`。`#project=1` 只是选中第一个项目，不会自动展开详情；点封面或「探索项目」进入。

**如果 `npm run build` 报 `EDEADLK`**：项目在 iCloud 同步的桌面上，未下载的文件读不了，`cpSync public -> out/images` 会失败。只改了文案或样式（没有新增图片）时，跳过完整 build，直接重新生成首页：

```sh
node -e "import('./scripts/project-stage.mjs').then(async m=>{const {readFileSync}=await import('node:fs');const {profile,projects}=JSON.parse(readFileSync('app/content.json','utf8'));m.buildStage(profile,projects)})"
```

这条不复制图片。新增图片时需要手动 `cp public/images/<新图>.png out/images/`。

## 2. 架构：三件反直觉的事

1. **主预览不是 Next/React 生成的。** `scripts/build.mjs` 先生成 5 个静态页，最后调用 `buildStage()` 把 `out/index.html` 覆盖成真正的舞台页。工程里的 `app/page.tsx`、`components/ui/*` 等是遗留文件，不参与主预览——改它们不会有任何效果。
2. **`out/` 是产物。** 所有改动落在源码，不要直接编辑 `out/`。
3. **`/work/project-01/` 是旧式独立页，和首页内展开的 01–08 长页不同步。** 这是已知状态，不是 bug，不要"顺手修复"。当前的浏览路径是首页内展开。

## 3. 文件职责

| 文件 | 职责 |
|---|---|
| `app/content.json` | 全部文案、章节结构、图片引用 |
| `scripts/project-stage.mjs` | 生成首页与同页展开的项目长页 |
| `app/stage.css` | 首页、封面、详情、标注、响应式 |
| `scripts/stage.js` | 项目切换、展开/返回、视差、按钮遮挡、08 全览轨道 |
| `scripts/build.mjs` | 复制 public、生成 5 个静态页，再调 buildStage 覆盖首页 |

## 4. 设计约束

- 白底黑字，编辑式排版，主次清楚。避免模板化卡片、浅灰模块、密集研究报告式版面。
- 文案自然具体。偏好「食材状态可视化」这类明确的设计表达，不要「知道冰箱里有什么，也知道该先吃什么」式口号。
- **所有界面图统一 780×1688（比例 0.462）。** 引入新图前先验尺寸；比例不一致会让并排的手机忽大忽小。
- 05/06 的版式：UI 居中，左右各一段说明加一条细折线。手机端说明移到下方，用编号替代连接线。
- 05/06 的手机尺寸对齐 04。桌面参考宽度 `(min(65.36vw,1480px) - 2 * clamp(32px,3.5vw,72px)) / 3`。
- 图片不为了突出主视觉无限放大。
- 展开封面的中文标题：较小的实心深色黑体，不描边，不跨图片切成实心/描边两段。
- 下箭头：白圆底、深绿箭头，随滚动逐渐被正文遮住，不应一直浮在边界上。
- 08 界面全览：横向自由滚动，右侧露出下一张作为可滑动信号；左右按钮与计数器复用首页 rail 的视觉语言；无卡片框、无阴影容器、无自动播放、无灯箱。

## 5. 已经否决的方案 —— 不要再提

这些都试过并且被否掉了。不知道就会重来一遍，用户得再否一次。

- 大图、左图右文、中文描边标题（历史对话里有多版，全部作废）
- 938×1846 带外阴影留边的导出，配逐图 CSS 裁剪 hack。素材已重新导出为 780×1688 无留边，**不要再写这类裁剪覆盖**
- 08 全览做成轮播（一次一张），或纵向滚动驱动横向位移的滚动劫持
- 全览使用 `scroll-snap` 吸附：吸附点与左侧留白对不上，会把用户从第一张弹走，手感也黏，已移除
- 07 的数量调整前后对比、03 的取舍依据（Impact/Feasibility 矩阵与游戏化选型理由）：讨论过，**用户已决定不做**，不要主动提议

## 6. 证据边界 —— 硬规则

不得编造用户原话、访谈细节、测试数据或上线成效。

可以引用的事实只有下面这些：

- **用户访谈**：10 位同龄合租同学，半结构化访谈，便利抽样。按**提及人数**计，不是比例——写「10 位中有 7 位提到」，不写「70% 的用户」。主要提及：6 人一周买菜 ≤2 次且每次量大；7 人买到重复食材；4 人想起食材时人在上课、不在家；4 人因大份优惠多买吃不完；3 人试过记录但没动力坚持；3 人提到酸奶吃不完；4 人觉得可惜；仅 1 人买菜前会看冰箱。局限：未覆盖合租归属场景，单次访谈，样本来自同学。这场访谈未写进课程报告。
- **可用性测试**：课程报告记载 6 位参与者、任务式场景，发现首页认知过载、拖拽滑块在手机上是主要障碍；据此从 drag slider 改为 click-to-adjust 详情卡。
- **二手研究**：英国家庭贡献 70% 食物浪费、每年约 440 万吨可避免浪费、平均每户损失约 £1,000。
- **课程报告** `AUXT Coursework Group F (1).pdf` 关键页：第 5 页数字宠物提供积极反馈、避免内疚式引导；第 8 页宠物成长与长期参与；第 16 页低保真原型与 Impact/Feasibility 矩阵；第 17 页可用性测试发现的三个问题；第 18 页最终设计决定与预期影响。
- **游戏化是设计假设，不是研究发现。** 访谈只说明用户「没动力」维持记录，不说明积分和宠物就是答案。
- **没有任何实测的减少浪费比例、留存率或长期效果。** 不得写成成效。

## 7. 素材

- `~/Desktop/ai冰箱页面` —— 11 张界面的当前版本，全部 780×1688。`public/images` 就是从这里来的。
- `~/Desktop/ai冰箱` —— 原始素材、更早的导出、课程 PDF。

两个文件夹都在 iCloud 同步的桌面上，未下载的文件读不了（报 `EDEADLK` 或 deadlock）。使用前先在访达里确认已下载到本地。

## 8. 已知坑

- **完整 build 在 iCloud 上会失败**，绕法见第 1 节。
- **`app/stage.css` 是多轮追加覆盖的**，新规则写在文件尾部会覆盖前面的同名规则。清理时务必保持现有视觉与响应式行为，不要大范围重构。
- **05/06 的细折线是按比例静态定位的**，换图、改裁剪或改宽高比之后需要重新校准。
- 第一次 `git add -A` 很慢（要遍历 743MB 的 node_modules，加上 iCloud 下载），几分钟属正常，不要中断。

## 9. 当前状态与边界

- **项目一 01–08 已定稿，用户满意。不要重构、不要"优化"文案、不要重排版式。**
- 项目二、三仍为占位内容。
- 未部署。本地改动不代表线上更新。
- Git 仓库：`bbbbbbb86/chenke-portfolio`（私有）。

**接手流程**：读这份文件 → `git log --oneline` 和 `git diff HEAD~1` 看最近改了什么 → 跑起来浏览一遍 → 用简短中文复述你理解的架构与约束 → **等用户明确指定下一项工作**。不要自行新增章节、重新设计或部署。
